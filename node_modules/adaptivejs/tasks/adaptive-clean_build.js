module.exports = function(grunt) {
    // Remove build directory contents
    grunt.registerTask('adaptive-clean_build', function() {
        grunt.file.delete('build/');
        grunt.file.mkdir('build/');
    });
};
