module.exports = function(grunt) {
    return {
        local: {
            options: {
                urls: ['http://localhost:' + (grunt.option('p') || 8888) + '/tests/runner/'],
                grep: grunt.option('filter') || ''
            }
        },
        ci: {
            options: {
                reporter: 'xunit',
                output: process.env.CIRCLE_TEST_REPORTS + 'integration/results.xml',
                urls: ['http://localhost:' + (grunt.option('p') || 8888) + '/tests/runner/']
            }
        }
    };
};
