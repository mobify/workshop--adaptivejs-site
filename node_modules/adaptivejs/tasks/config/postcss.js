module.exports = function(grunt) {
    return {
        preload: {
            options: {
                map: false,
                processors: [
                    require('autoprefixer-core')({ browsers: ['iOS >= 6.0', 'Android >= 2.3', 'last 4 ChromeAndroid versions'] }),
                    require('cssnano')()
                ]
            },
            files: {
                'build/preload.min.css': 'app/preload/*.css'
            }
        }
    };
};
