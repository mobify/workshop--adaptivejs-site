define(function() {

    /**
     *  If the mobify-path cookie is present, then we are in debug mode
     */
    var isDebug = function(cookie) {
        cookie = cookie || document.cookie;

        var match = /mobify-path=([^&;]*)/.exec(cookie);

        return !!match;
    };

    return isDebug;
});
